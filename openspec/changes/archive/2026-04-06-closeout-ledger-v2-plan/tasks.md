# Tasks: closeout-ledger-v2-plan

## 1. Spec and scope freeze

- [x] Add closeout requirement delta for CI baseline completeness.

## 2. CI implementation

- [x] Add GitHub Actions workflow for install, db:generate, typecheck, lint, test, and build.
- [x] Ensure workflow uses workspace-compatible pnpm setup.

## 3. Final verification

- [x] Re-run local `typecheck`, `lint`, `test`, and `build`.
- [x] Perform final code review summary with risks and residual gaps.

## 4. OpenSpec closure

- [x] Add acceptance and context packet artifacts.
- [ ] Validate and archive this change.
